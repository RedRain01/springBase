server:
  port: 8087

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/whyt?userUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.mysqlserver.entity
#showSql
logging:
  level:
    com:
      example:
        mapper : debug